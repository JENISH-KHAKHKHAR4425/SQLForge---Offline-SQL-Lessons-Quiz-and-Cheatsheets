package com.example.offlinesqllearner.data

object QuestionRepo {

    val questions: List<Question> = listOf(
        Question(1, 1, "What does SQL stand for?",
            listOf("Structured Query Language", "Simple Query Logic", "Sequential Query Language", "Structured Question Language"),
            0, "SQL stands for Structured Query Language."),
        Question(2, 1, "Which category does CREATE TABLE belong to?",
            listOf("DML", "DDL", "DCL", "TCL"),
            1, "CREATE, ALTER and DROP are Data Definition Language (DDL) commands."),
        Question(3, 1, "Which symbol ends most SQL statements?",
            listOf(":", ";", ".", "#"),
            1, "SQL statements are terminated with a semicolon ( ; )."),

        Question(4, 2, "Which keyword retrieves data from a table?",
            listOf("GET", "FETCH", "SELECT", "SHOW"),
            2, "SELECT is used to query and retrieve data."),
        Question(5, 2, "What does SELECT * do?",
            listOf("Selects zero rows", "Selects all columns", "Multiplies two columns", "Deletes all rows"),
            1, "The asterisk (*) is a wildcard meaning 'all columns'."),
        Question(6, 2, "How do you rename a column in the result set?",
            listOf("RENAME", "AS", "ALIAS TO", "NAME("),
            1, "The AS keyword creates an alias, e.g. SELECT name AS full_name."),

        Question(7, 3, "Which clause filters rows based on a condition?",
            listOf("HAVING", "WHERE", "GROUP BY", "ORDER BY"),
            1, "WHERE filters individual rows before any grouping happens."),
        Question(8, 3, "What does LIKE 'A%' match?",
            listOf("Exactly the letter A", "Any value ending in A", "Any value starting with A", "Values containing no letters"),
            2, "% is a wildcard for any number of characters, so 'A%' matches anything starting with A."),
        Question(9, 3, "Which operator checks a range of values?",
            listOf("IN", "BETWEEN", "LIKE", "IS"),
            1, "BETWEEN checks whether a value falls within a range, inclusive."),
        Question(10, 3, "How do you check for missing/empty values?",
            listOf("= NULL", "== NULL", "IS NULL", "NULL()"),
            2, "NULL can't be compared with =, so IS NULL / IS NOT NULL must be used."),

        Question(11, 4, "Which clause sorts the result set?",
            listOf("SORT BY", "ORDER BY", "GROUP BY", "ARRANGE BY"),
            1, "ORDER BY sorts rows, ascending by default."),
        Question(12, 4, "Which keyword limits the number of rows returned?",
            listOf("TOP", "LIMIT", "MAX", "ROWCOUNT"),
            1, "In SQLite/MySQL/Postgres, LIMIT restricts the number of returned rows."),
        Question(13, 4, "What does DESC do inside ORDER BY?",
            listOf("Describes the table", "Sorts descending", "Deletes the column", "Sorts randomly"),
            1, "DESC sorts results from highest to lowest (descending order)."),

        Question(14, 5, "Which statement adds a new row to a table?",
            listOf("ADD", "INSERT INTO", "NEW ROW", "CREATE ROW"),
            1, "INSERT INTO table (...) VALUES (...) adds a new row."),
        Question(15, 5, "What happens if UPDATE is run without a WHERE clause?",
            listOf("Nothing happens", "It updates one random row", "It updates every row in the table", "It throws a syntax error"),
            2, "Without WHERE, UPDATE (and DELETE) applies to every row in the table — a common and dangerous mistake."),
        Question(16, 5, "Which statement removes rows from a table?",
            listOf("REMOVE FROM", "DELETE FROM", "DROP FROM", "CLEAR TABLE"),
            1, "DELETE FROM table WHERE ... removes matching rows."),

        Question(17, 6, "Which constraint prevents a column from being empty?",
            listOf("UNIQUE", "NOT NULL", "DEFAULT", "CHECK"),
            1, "NOT NULL requires that a column always has a value."),
        Question(18, 6, "Which constraint ensures no two rows share the same value in a column?",
            listOf("PRIMARY KEY only", "UNIQUE", "DEFAULT", "INDEX"),
            1, "UNIQUE (and PRIMARY KEY, which is unique + not null) prevents duplicate values."),
        Question(19, 6, "What does CHECK(salary > 0) do?",
            listOf("Deletes negative salaries automatically", "Validates that salary must be greater than 0", "Creates an index on salary", "Nothing, it's just a comment"),
            1, "CHECK constraints validate that a condition is true for every row."),

        Question(20, 7, "What uniquely identifies a row in a table?",
            listOf("FOREIGN KEY", "PRIMARY KEY", "INDEX", "ALIAS"),
            1, "A PRIMARY KEY uniquely identifies each row and cannot be NULL or duplicated."),
        Question(21, 7, "What does a FOREIGN KEY do?",
            listOf("Encrypts a column", "References the primary key of another table", "Automatically sorts a table", "Speeds up all queries"),
            1, "A FOREIGN KEY links a column to a primary key in another table, enforcing referential integrity."),

        Question(22, 8, "Which JOIN returns only matching rows from both tables?",
            listOf("LEFT JOIN", "INNER JOIN", "CROSS JOIN", "SELF JOIN"),
            1, "INNER JOIN returns only rows where the join condition matches in both tables."),
        Question(23, 8, "Which JOIN keeps all rows from the left table even without a match?",
            listOf("INNER JOIN", "RIGHT JOIN", "LEFT JOIN", "FULL JOIN"),
            2, "LEFT JOIN keeps every row from the left table, filling unmatched columns with NULL."),
        Question(24, 8, "In 'FROM employees e JOIN departments d', what is 'e'?",
            listOf("A function", "A table alias", "A column", "A keyword"),
            1, "'e' is an alias — a short nickname for the employees table used in the rest of the query."),

        Question(25, 9, "Which function counts the number of rows?",
            listOf("SUM()", "COUNT()", "TOTAL()", "ROWS()"),
            1, "COUNT() returns the number of rows matching a condition."),
        Question(26, 9, "GROUP BY is typically used with which type of function?",
            listOf("String functions", "Aggregate functions", "Date functions", "Window resizing functions"),
            1, "GROUP BY groups rows so aggregate functions like SUM, AVG and COUNT can summarize each group."),
        Question(27, 9, "Why can't you filter an aggregate with WHERE?",
            listOf("WHERE is deprecated", "WHERE runs before grouping happens, so groups don't exist yet", "WHERE only works on TEXT columns", "You actually can, HAVING is never needed"),
            1, "WHERE filters rows before grouping; HAVING filters groups after aggregation is calculated."),

        Question(28, 10, "What is a subquery?",
            listOf("A query stored permanently on disk", "A SELECT statement nested inside another query", "A shortcut for JOIN", "A type of index"),
            1, "A subquery is a query nested inside another SQL statement, often in parentheses."),
        Question(29, 10, "Where can a subquery appear?",
            listOf("Only in WHERE", "Only in SELECT", "In WHERE, FROM, or SELECT clauses", "Nowhere, subqueries are invalid SQL"),
            2, "Subqueries are flexible and can appear in WHERE, FROM (as a derived table), or the SELECT list."),

        Question(30, 11, "What is a VIEW?",
            listOf("A physical copy of a table", "A saved query that acts like a virtual table", "A type of index", "A backup file"),
            1, "A VIEW stores a query definition, not the data itself, and re-runs it each time it's queried."),
        Question(31, 11, "How do you remove a view?",
            listOf("DELETE VIEW", "REMOVE VIEW", "DROP VIEW", "CLEAR VIEW"),
            2, "DROP VIEW view_name; removes a view definition."),

        Question(32, 12, "What is the main purpose of an index?",
            listOf("To encrypt data", "To speed up data lookups", "To add new columns", "To back up the database"),
            1, "Indexes speed up searching, filtering, and joining by avoiding full table scans."),
        Question(33, 12, "What is a downside of adding too many indexes?",
            listOf("SELECT queries become impossible", "INSERT/UPDATE/DELETE become slower and storage increases", "Tables can no longer be joined", "It disables WHERE clauses"),
            1, "Every index must be updated on writes, so more indexes mean slower writes and more storage."),

        Question(34, 13, "What does COMMIT do?",
            listOf("Cancels the current transaction", "Saves all changes made in the transaction permanently", "Deletes the table", "Creates a backup"),
            1, "COMMIT makes all changes in the current transaction permanent."),
        Question(35, 13, "What does ROLLBACK do?",
            listOf("Repeats the last statement", "Undoes changes made since the transaction began", "Speeds up the query", "Renames a table"),
            1, "ROLLBACK reverts the database to the state before the transaction started."),
        Question(36, 13, "What does the 'A' in ACID stand for?",
            listOf("Availability", "Authentication", "Atomicity", "Aggregation"),
            2, "Atomicity means all statements in a transaction succeed together, or none do."),

        Question(37, 14, "What does a CTE (WITH clause) provide?",
            listOf("A permanent new table", "A named, temporary result set for one query", "A way to delete data faster", "A security permission"),
            1, "A CTE defines a temporary, named result set that exists only for the duration of the query."),
        Question(38, 14, "What do window functions do that GROUP BY doesn't?",
            listOf("They collapse rows into one row per group", "They calculate across related rows without collapsing them", "They only work on TEXT columns", "They are identical to GROUP BY"),
            1, "Window functions compute values (like rank or running totals) while keeping every individual row visible."),

        Question(39, 8, "What is returned by a CROSS JOIN?",
            listOf("Only matching rows", "The Cartesian product of both tables", "Nothing, it's invalid SQL", "Only the left table's rows"),
            1, "CROSS JOIN pairs every row of one table with every row of the other."),
        Question(40, 9, "Which function finds the highest value in a column?",
            listOf("TOP()", "MAX()", "HIGH()", "LAST()"),
            1, "MAX() returns the largest value in a set."),
        Question(41, 3, "Which operator matches a value against a list?",
            listOf("IN", "LIST", "SET", "OF"),
            0, "IN checks whether a value matches any value in a given list."),
        Question(42, 5, "Which clause is essential to safely target specific rows in UPDATE/DELETE?",
            listOf("ORDER BY", "WHERE", "LIMIT", "GROUP BY"),
            1, "WHERE ensures only the intended rows are changed or removed.")
    )

    fun forLesson(lessonId: Int): List<Question> = questions.filter { it.lessonId == lessonId }

    fun randomSet(count: Int = 10): List<Question> = questions.shuffled().take(count)
}
